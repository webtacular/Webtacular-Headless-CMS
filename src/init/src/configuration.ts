import { ErrorInterface, GlobalRoleObjectInterface } from "../../core/interfaces";

export const globalRoleObject = async(): Promise<GlobalRoleObjectInterface | ErrorInterface> => {
    return new Promise(async(resolve, reject) => {});
}